name = "push-service"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
VAPID_SUBJECT = "mailto:admin@jumpedia.app"

# Secrets (set via: wrangler secret put VAPID_PUBLIC_KEY etc):
# VAPID_PUBLIC_KEY
# VAPID_PRIVATE_KEY
# PUSH_WORKER_SECRET
